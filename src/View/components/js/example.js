// components/js/example.js
// Exemple de script supplémentaire pour démonstration

document.addEventListener('DOMContentLoaded', function() {
    console.log('Script example.js chargé avec succès !');

    // Exemple d'interaction simple
    const body = document.body;
    if (body) {
        console.log('Page chargée avec le système JS flexible');
    }
});